{%- comment -%}Seven Meridian Header — Announcement bar + Navigation{%- endcomment -%}

<style>
  @keyframes announcement-shimmer {
    0% { background-position: -200% center; }
    100% { background-position: 200% center; }
  }
  .announcement-bar {
    background: var(--color-primary);
    color: #fff;
    text-align: center;
    padding: 8px 16px;
    font-size: 13px;
    letter-spacing: 0.05em;
    overflow: hidden;
    position: relative;
  }
  .announcement-bar::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent 0%, rgba(197,165,114,0.15) 50%, transparent 100%);
    background-size: 200% 100%;
    animation: announcement-shimmer 4s ease-in-out infinite;
    pointer-events: none;
  }
  .announcement-bar a {
    color: var(--color-accent);
    text-decoration: underline;
  }
  .site-header {
    background: var(--color-background);
    border-bottom: 1px solid var(--color-border);
    padding: 16px 0;
    position: sticky;
    top: 0;
    z-index: 100;
  }
  .header-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .header-logo {
    display: flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
    color: var(--color-primary);
  }
  .header-logo__text {
    font-family: var(--font-heading);
    font-size: 20px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .header-nav {
    display: flex;
    gap: 32px;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .header-nav a {
    text-decoration: none;
    color: var(--color-text);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.03em;
    transition: color 0.2s;
  }
  .header-nav a:hover {
    color: var(--color-accent);
  }
  .header-actions {
    display: flex;
    align-items: center;
    gap: 20px;
  }
  .header-actions a {
    color: var(--color-text);
    text-decoration: none;
    position: relative;
  }
  .cart-count {
    position: absolute;
    top: -8px;
    right: -10px;
    background: var(--color-accent);
    color: #fff;
    font-size: 10px;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
  }
  .header-shop-btn {
    display: inline-block;
    padding: 8px 18px;
    background: var(--color-accent);
    color: var(--color-primary);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s;
  }
  .header-shop-btn:hover {
    opacity: 0.9;
    transform: scale(1.03);
    color: var(--color-primary);
  }
  .mobile-menu-toggle {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }
  @media (max-width: 768px) {
    .header-nav { display: none; }
    .mobile-menu-toggle { display: block; }
    .header-shop-btn { display: none; }
  }
</style>

{%- if settings.announcement_text != blank -%}
  <div class="announcement-bar" role="banner">
    {%- if settings.announcement_link != blank -%}
      <a href="{{ settings.announcement_link }}">{{ settings.announcement_text }}</a>
    {%- else -%}
      {{ settings.announcement_text }}
    {%- endif -%}
  </div>
{%- endif -%}

<header class="site-header" role="banner">
  <div class="header-inner">
    <a href="/" class="header-logo" aria-label="{{ shop.name }} — home">
      {% render 'icon-meridian-logo', width: 36, height: 36, dot_color: '#0B2545', center_color: '#C5A572' %}
      <span class="header-logo__text">Seven Meridian</span>
    </a>

    <nav aria-label="Main navigation">
      <ul class="header-nav">
        {%- for link in linklists[section.settings.menu].links -%}
          <li>
            <a href="{{ link.url }}"{% if link.current %} aria-current="page"{% endif %}>
              {{ link.title }}
            </a>
          </li>
        {%- endfor -%}
      </ul>
    </nav>

    <div class="header-actions">
      <a href="/account" aria-label="Account">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
      </a>
      <a href="/search" aria-label="Search">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/></svg>
      </a>
      <a href="/collections/all" class="header-shop-btn">Shop Now</a>
      <a href="/cart" class="cart-link" id="cart-toggle" aria-label="Cart">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 01-8 0"/></svg>
        {%- if cart.item_count > 0 -%}
          <span class="cart-count">{{ cart.item_count }}</span>
        {%- endif -%}
      </a>
      <button class="mobile-menu-toggle" aria-label="Open menu" aria-expanded="false">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
      </button>
    </div>
  </div>
</header>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    var cartToggle = document.getElementById('cart-toggle');
    if (cartToggle) {
      cartToggle.addEventListener('click', function(e) {
        var drawer = document.getElementById('cart-drawer');
        if (drawer) {
          e.preventDefault();
          drawer.classList.add('is-open');
          document.body.classList.add('cart-drawer-open');
        }
      });
    }
  });
</script>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "Main menu",
      "default": "main-menu"
    }
  ]
}
{% endschema %}
